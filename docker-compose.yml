version: "3.8"

services:
  backend:
    build: .
    container_name: api_backend
    command: uvicorn main:app --reload --port 8000 --host=0.0.0.0
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./:/app